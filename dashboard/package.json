{
  "name": "homebox-dashboard",
  "version": "0.0.0",
  "private": true,
  "description": "Podhouse dashboard - reads the module tree and the Docker socket, serves one page",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test/yaml.test.js && node --test \"test/*.test.js\""
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "_versionNote": "Not the Podhouse version. The VERSION file at the repo root is the single source of truth \u2014 install.sh reads it, the CLI reads it, server.js reads it. Two numbers that must agree eventually will not."
}
