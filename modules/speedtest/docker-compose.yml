# Speedtest — a Podhouse module
#
# Evidence for the conversation with the ISP.

x-homebox:
  id: speedtest
  added_at: "2026-09-09"
  title: "Speedtest Tracker"
  # tagline, description, source, docs, license: the project's own, via scripts/upstream-meta.js
  tagline: "Speedtest Tracker is a self-hosted application that monitors the performance and uptime of your internet connection."
  description: "Speedtest Tracker is a self-hosted application that monitors the performance and uptime of your internet connection."
  source: "https://github.com/alexjustesen/speedtest-tracker"
  docs: "https://speedtest-tracker.dev"
  license: "MIT"
  icon: "speedtest-tracker.png"
  category: "network"
  required: false
  default: false
  ram: "~250MB"
  theme:
    emoji: "📶"
    color: "#0ea5e9"
    bg: "rgba(14,165,233,0.12)"
  tips:
    - "Hourly is plenty. Every fifteen minutes mostly measures the speed tests fighting each other for the line"
    - "Pin a specific test server in Settings, otherwise a change of server looks like a change of speed"
    - "APP_KEY is generated at install and encrypts the stored results; replacing it makes the existing history unreadable"
  env_vars:
    SPEEDTEST_APP_KEY:
      label: "Encryption key"
      type: "secret"
      # Laravel's APP_KEY, so exactly 32 bytes in STANDARD base64. The default
      # 24 url-safe bytes fail twice over: too short for AES-256, and the
      # url-safe alphabet is not the one PHP decodes — which it does not
      # refuse, it just decodes to something else.
      bytes: 32
      format: "base64"
      config_editable: false
      dangerous: true
  services:
    speedtest:
      friendly_name: "Speedtest Tracker"
      color: "#0ea5e9"
      description: "Scheduled connection speed tests"
      icon: "speedtest-tracker.png"
      port_map: 8765
      container_port: 80
      url_scheme: "http"
      first_login: "Ships with admin@example.com / password. Change both on the first visit."

services:
  speedtest:
    image: lscr.io/linuxserver/speedtest-tracker:1.14.5
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      # Starts as root, takes ownership of its data directory and drops to its
      # own user. Without these five it either dies on chown or stays root.
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
      - SETGID
      - SETUID
    container_name: speedtest
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-UTC}
      # Laravel refuses to boot without one, and it encrypts stored results.
      # base64: prefix is required; the generated value supplies the rest.
      - APP_KEY=base64:${SPEEDTEST_APP_KEY:-}
      - APP_URL=${SPEEDTEST_URL:-http://${HB_HOST_ADDRESS:-localhost}:8765}
      - DB_CONNECTION=sqlite
      - SPEEDTEST_SCHEDULE=0 * * * *
    ports:
      - "8765:80"
    volumes:
      - ${HB_ROOT:-/opt/podhouse}/modules/speedtest/config/speedtest:/config
    networks:
      - homebox_proxy
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '2.00'
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    restart: always

networks:
  homebox_proxy:
    external: true
